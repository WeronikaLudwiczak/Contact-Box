{% extends '::base.html.twig' %}

{% block body -%}
    {% include('weronikaContactBoxBundle::menu.html.twig') %}
    <h2 class= "well" ><center>Email List</center></h2>

    <table class="table table-striped table-hover ">
        <thead>
        <tr>
            <th>Id</th>
            <th>Email Address</th>
            <th>Email Type</th>
            <th>User</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('email_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.emailAddress }}</td>
                <td>{{ entity.emailType }}</td>
                <td>{{ entity.user }}</td>
                <td>


                    <a href="{{ path('email_show', { 'id': entity.id }) }}" class="btn btn-primary btn-sm">show</a>


                    <a href="{{ path('email_edit', { 'id': entity.id }) }}" class="btn btn-info btn-sm">edit</a>



                    <a href="{{ path('email_delete', { 'id': entity.id }) }}" onclick="return confirm('Are you sure do you want to delet the email ?') "class="btn btn-warning btn-sm">Delete</a>


                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>

        <a href="{{ path('email_new') }}">
            Create a new Email
        </a>

    </ul>
{% endblock %}


